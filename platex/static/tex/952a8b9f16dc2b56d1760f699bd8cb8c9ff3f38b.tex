\documentclass{article}
\usepackage{tikz, comment}
\usepackage{pifont}
\usepackage{fontspec}
\usetikzlibrary{arrows, decorations.markings, decorations.pathreplacing}
\begin{comment}
:Title: Not defined yet
:Slug: No name yet

Description Here.........
\end{comment}
\begin{document}\centering

\def\rx{1.25}
\def\ry{1.25}
\def\a{1}

\begin{tikzpicture}[>=latex, xscale=.5*(\rx), yscale=.5*(\ry)] 

\draw[xstep=1cm,ystep=1cm,color=gray!20] ({-3*(\a)-0.01}, {-3*(\a)}) grid ({3*(\a)}, {3*(\a)});

\draw[->] ({-3*(\a)}, 0) -- ({3*(\a)+0.3/(\rx)},0) node[right] {\small$x$};
\draw[->] (0,{-3*(\a)}) -- (0, {3*(\a)+0.3/(\ry)}) node[above] {\small$y$};

\clip[] ({-3*(\a)}, {-3*(\a)}) rectangle ({3*(\a)}, {3*(\a)});

\foreach \p/\q in {{-1-0.1}/-20, {-1+0.1}/20}
\draw[purple, thick, samples=200, smooth, domain=\p:\q, variable=\t] 
	plot ({3*(\a)*(\t)/(1+(\t)^3)}, {3*(\a)*(\t)^2/(1+(\t)^3)}) -- (0, 0);

\foreach \p/\q in {-10/10}
\draw[densely dotted, samples=100, smooth, domain=\p:\q, variable=\x] 
	plot ({\x}, {- \x - \a});
\node[below, opacity=0.5, scale=0.6, rotate=-45] at ({-0.5*(\a)}, {0.5*(\a) - (\a)}) {$x+y+a=0$};

\foreach \p/\q in {-10/10}
\draw[teal, dashdotted, samples=100, smooth, domain=\p:\q, variable=\x] 
	plot ({\x}, {\x});

\draw ({\a}, {2pt/(\ry)}) -- ({\a}, {-2pt/(\ry)}) node[anchor=north, scale=0.6] {$a$};

\draw ({2pt/(\rx)}, {\a}) -- ({-2pt/(\rx)}, {\a}) node[anchor=east, scale=0.6] {$a$};

\foreach \p/\q in {0/0, {4^(1/3)*(\a)}/{2^(1/3)*(\a)}, {2^(1/3)*(\a)}/{4^(1/3)*(\a)}} 
\draw[purple, fill, xscale=1/\rx, yscale=1/\ry]  ({(\p)*(\rx)}, {(\q)*(\ry)}) circle({0.05});	

\node[scale=0.8] at ({-0.3/(\rx)}, {-0.3/(\ry)}) {$O$};

\end{tikzpicture}
\end{document}