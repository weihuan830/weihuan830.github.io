\documentclass{article}
\usepackage{tikz, comment}
\usepackage{pifont}
\usepackage{fontspec}
\usetikzlibrary{arrows, decorations.markings, decorations.pathreplacing}
\begin{comment}
:Title: Not defined yet
:Slug: No name yet

Description Here.........
\end{comment}
\begin{document}\centering

\def\rx{2}
\def\ry{2}

\begin{tikzpicture}[>=latex, xscale=.5*(\rx), yscale=.5*(\ry)] 

\draw[xstep=1cm,ystep=1cm,color=gray!20] ({-4-0.01}, -1) grid (3, 2);

\draw[->] (-4, 0) -- ({3+0.3/(\rx)},0) node[right] {\small$x$};
\draw[->] (0,-1) -- (0, {2+0.3/(\ry)}) node[above] {\small$y$};

\clip[] (-4, -1) rectangle (3, 2);

\foreach \p/\q in {0/0.9, 1.1/8}
\draw[purple, thick, samples=100, smooth, domain=\p:\q, variable=\t] 
	plot ({(\t)^2/(1-(\t)^2)}, {1/((\t)^2+1)}); 

\draw[densely dotted, opacity=0.5] (-4, 0.5) -- (3, 0.5);
\node[above, opacity=0.5, scale=0.6] at (-3, 0.5) {$y=\frac{1}{2}$};

\foreach \x in {-1}
\draw (\x, {2pt/(\ry)}) -- (\x, {-2pt/(\ry)}) node[anchor=north, scale=0.6] {$\x$};

\foreach \y in {1}
\draw ({2pt/(\rx)}, \y) -- ({-2pt/(\rx)}, \y) node[anchor=east, scale=0.6] {$\y$};

\foreach \p/\q in {}
\draw[purple, fill=white, xscale=1/\rx, yscale=1/\ry]  ({(\p)*(\rx)}, {(\q)*(\ry)}) circle({0.075});	

\foreach \p/\q in {}
\draw[purple, fill, xscale=1/\rx, yscale=1/\ry]  ({(\p)*(\rx)}, {(\q)*(\ry)}) circle({0.05});	

\node[scale=0.8] at ({-0.3/(\rx)}, {-0.3/(\ry)}) {$O$};

\end{tikzpicture}
\end{document}