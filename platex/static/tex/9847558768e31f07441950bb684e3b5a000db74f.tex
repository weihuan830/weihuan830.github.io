\documentclass{article}
\usepackage{tikz, comment}
\usepackage{pifont}
\usepackage{fontspec}
\usetikzlibrary{arrows, decorations.markings, decorations.pathreplacing}
\begin{comment}
:Title: Not defined yet
:Slug: No name yet

Description Here.........
\end{comment}
\begin{document}\centering

\def\rx{1.5}
\def\ry{1.5}

\begin{tikzpicture}[>=latex, xscale=.5*(\rx), yscale=.5*(\ry)] 

\draw[xstep=1cm,ystep=1cm,color=gray!20] ({-2-0.01}, -1) grid (3, 5);

\draw[->] (-2, 0) -- ({3+0.3/(\rx)},0) node[right] {\small$x$};
\draw[->] (0,-1) -- (0, {5+0.3/(\ry)}) node[above] {\small$y$};

\clip[] (-2, -1) rectangle (3, 5);

\foreach \p/\q in {-3/3}
\draw[purple, thick, samples=100, smooth, domain=\p:\q, variable=\t] 
	plot ({\t + e^(-\t)}, {2*(\t) + e^(-2*(\t))});

\foreach \p/\q in {0/3}
\draw[densely dotted, samples=100, smooth, domain=\p:\q, variable=\x] 
	plot ({\x}, {2*(\x)});
\node[above, opacity=0.5, scale=0.6, rotate=63.4349] at (0.8, {2*0.8}) {$y=2x$};

\foreach \x in {1}
\draw (\x, {2pt/(\ry)}) -- (\x, {-2pt/(\ry)}) node[anchor=north, scale=0.6] {$\x$};

\foreach \y in {1}
\draw ({2pt/(\rx)}, \y) -- ({-2pt/(\rx)}, \y) node[anchor=east, scale=0.6] {$\y$};

\foreach \p/\q in {}
\draw[purple, fill=white, xscale=1/\rx, yscale=1/\ry]  ({(\p)*(\rx)}, {(\q)*(\ry)}) circle({0.075});	

\foreach \p/\q in {1/1}
\draw[purple, fill, xscale=1/\rx, yscale=1/\ry]  ({(\p)*(\rx)}, {(\q)*(\ry)}) circle({0.05});	

\node[scale=0.8] at ({-0.3/(\rx)}, {-0.3/(\ry)}) {$O$};

\end{tikzpicture}
\end{document}