\documentclass{article}
\usepackage{tikz, comment}
\usepackage{pifont}
\usepackage{fontspec}
\usetikzlibrary{arrows, decorations.markings, decorations.pathreplacing}
\begin{comment}
:Title: Not defined yet
:Slug: No name yet

Description Here.........
\end{comment}
\begin{document}\centering

\def\r{1.333}

\begin{tikzpicture}[>=latex, xscale=.5*(\r), yscale=.5*(\r)] 

\draw[xstep=1cm,ystep=1cm,color=gray!20] (-6, -4) grid (6, 4);

\draw[->] (-6, 0) -- ({6+0.3/(\r)},0) node[right] {\small$x$};
\draw[->] (0,-4) -- (0, {4+0.3/(\r)}) node[above] {\small$y$};

\clip[] (-6, -4) rectangle (6, 4);

\foreach \a in {0.5, 1, 2}
{
\foreach \p/\q in {-2/6}
\draw[purple, thick, samples=100, smooth, domain=\p:\q, variable=\x] 
	plot ({\x}, {(\x)*e^(-(\x)/(\a))});
}

\foreach \a in {-0.5, -1, -2}
{
\foreach \p/\q in {-6/2}
\draw[orange, thick, samples=100, smooth, domain=\p:\q, variable=\x] 
	plot ({\x}, {(\x)*e^(-(\x)/(\a))});
}

\node[fill=white, scale=0.4, rotate=77] at ({-1.55}, {(-1.55)*e^(-(-1.55)/(2))}) {$a=2$};
\node[fill=white, scale=0.4, rotate=80] at ({-1.1}, {(-1.1)*e^(-(-1.1)/(1))}) {$a=1$};
\node[fill=white, scale=0.4, rotate=82] at ({-0.75}, {(-0.75)*e^(-(-0.75)/(0.5))}) {$a=0.5$};

\node[fill=white, scale=0.4, rotate=77] at ({1.55}, {(1.55)*e^(-(1.55)/(-2))}) {$a=-2$};
\node[fill=white, scale=0.4, rotate=80] at ({1.1}, {(1.1)*e^(-(1.1)/(-1))}) {$a=-1$};
\node[fill=white, scale=0.4, rotate=82] at ({0.75}, {(0.75)*e^(-(0.75)/(-0.5))}) {$a=-0.5$};

\foreach \a in {0.5, 1, 2}
{
\draw[purple, fill]  ({\a}, {(\a)/e}) circle({0.05/(\r)});	
\draw[purple, fill]  ({2*(\a)}, {2*(\a)/e^2}) circle({0.05/(\r)});	
}

\foreach \a in {-0.5, -1, -2}
{
\draw[orange, fill]  ({\a}, {(\a)/e}) circle({0.05/(\r)});	
\draw[orange, fill]  ({2*(\a)}, {2*(\a)/e^2}) circle({0.05/(\r)});	
}

\foreach \x in {1}
\draw (\x, {2pt/(\r)}) -- (\x, {-2pt/(\r)}) node[anchor=north, scale=0.6] {$\x$};

\foreach \y in {1}
\draw ({2pt/(\r)}, \y) -- ({-2pt/(\r)}, \y) node[anchor=east, scale=0.6] {$\y$};

\node[scale=0.8] at ({0.3/(\r)}, {-0.3/(\r)}) {$O$};

\end{tikzpicture}
\end{document}