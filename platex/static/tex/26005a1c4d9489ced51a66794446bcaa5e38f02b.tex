\documentclass{article}
\usepackage{tikz, comment}
\usepackage{pifont}
\usepackage{fontspec}
\usetikzlibrary{arrows, decorations.markings, decorations.pathreplacing}
\begin{comment}
:Title: Not defined yet
:Slug: No name yet

Description Here.........
\end{comment}
\begin{document}\centering

\def\r{1.25}

\begin{tikzpicture}[>=latex, xscale=.5*(\r), yscale=.5*(\r)] 

\draw[xstep=1cm,ystep=1cm,color=gray!20] (-4, -4) grid (4, 4);

\draw[->] (-4, 0) -- ({4+0.3/(\r)},0) node[right] {\small$x$};
\draw[->] (0,-4) -- (0, {4+0.3/(\r)}) node[above] {\small$y$};

\clip[] (-4, -4) rectangle (4, 4);

\foreach \a in {0.5, 1, 1.5}
{
\foreach \p/\q in {-4/-0.05, 0.05/4}
\draw[purple, thick, samples=100, smooth, domain=\p:\q, variable=\x] 
	plot ({\x}, {(\x) + (\a)^2/(\x)});
}

\node[fill=white, scale=0.4] at (0.3, {(0.3) + (0.5)^2/(0.3)+0.3}) {$a=0.5$};
\node[fill=white, scale=0.4] at (0.5, {(0.5) + (1)^2/(0.5)}) {$a=1$};
\node[fill=white, scale=0.4] at (1, {(1) + (1.5)^2/(1)+0.2}) {$a=1.5$};
	
\foreach \a in {0.5, 1, 1.5}
{
\draw[purple, fill]  ({\a}, {(\a) + (\a)^2/(\a)}) circle({0.05/(\r)});	
\draw[purple, fill]  ({-\a}, {(-\a) + (\a)^2/(-\a)}) circle({0.05/(\r)});	
}

\foreach \x in {-1, 1}
\draw (\x, {2pt/(\r)}) -- (\x, {-2pt/(\r)}) node[anchor=north, scale=0.6] {$\x$};

\foreach \y in {1}
\draw ({2pt/(\r)}, \y) -- ({-2pt/(\r)}, \y) node[anchor=east, scale=0.6] {$\y$};
\foreach \y in {-1}
\draw ({-2pt/(\r)}, \y) -- ({2pt/(\r)}, \y) node[anchor=west, scale=0.6] {$\y$};

\draw[dashed] (-4, -4) -- (4, 4);

\node[scale=0.8] at ({0.3/(\r)}, {-0.3/(\r)}) {$O$};

\end{tikzpicture}
\end{document}