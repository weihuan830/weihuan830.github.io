\documentclass{article}
\usepackage{tikz, comment}
\usepackage{pifont}
\usepackage{fontspec}
\usetikzlibrary{arrows, decorations.markings, decorations.pathreplacing}
\begin{comment}
:Title: Not defined yet
:Slug: No name yet

Description Here.........
\end{comment}
\begin{document}\centering

\def\r{1.333}

\begin{tikzpicture}[>=latex, xscale=.5*(\r), yscale=.5*(\r)] 

\draw[xstep=1cm,ystep=1cm,color=gray!20] (-3, -4) grid (3, 4);

\draw[->] (-3, 0) -- ({3+0.3/(\r)},0) node[right] {\small$x$};
\draw[->] (0,-4) -- (0, {4+0.3/(\r)}) node[above] {\small$y$};

\clip[] (-3, -4-0.1) rectangle (3, 4+0.1);

\foreach \a in {0.25, 1, 2, 4, 8}
{
\foreach \p/\q in {0/2*pi}
\draw[purple, thick, samples=100, smooth, domain=\p:\q, variable=\t] 
	plot ({cos(\t r)}, {cos(\t r) + sqrt(\a)*sin(\t r)});
}

\node[fill=white, scale=0.4] at ({0.1}, {(0.1) + sqrt((0.25)*(1-(0.1)^2))}) {$a=\frac{1}{4}$};
\node[fill=white, scale=0.4] at ({0.1}, {(0.1) + sqrt((1)*(1-(0.1)^2))}) {$a=1$};
\node[fill=white, scale=0.4] at ({0}, {(0) + sqrt((2)*(1-(0)^2))}) {$a=2$};
\node[fill=white, scale=0.4] at ({-0.1}, {(-0.1) + sqrt((4)*(1-(-0.1)^2))}) {$a=4$};
\node[fill=white, scale=0.4] at ({-0.3}, {(-0.3) + sqrt((8)*(1-(-0.3)^2))}) {$a=8$};
	
\foreach \a in {0, 0.25, 1, 2, 4, 8}
{
\draw[purple, fill]  ({sqrt(1/(1+\a))}, {sqrt(1/(1+\a)) + sqrt((\a)*(1-(sqrt(1/(1+\a)))^2))}) circle({0.05/(\r)});	
\draw[purple, fill]  ({-sqrt(1/(1+\a))}, {-sqrt(1/(1+\a)) - sqrt((\a)*(1-(-sqrt(1/(1+\a)))^2))}) circle({0.05/(\r)});
}

\foreach \x in {1}
\draw (\x, {2pt/(\r)}) -- (\x, {-2pt/(\r)}) node[anchor=north, scale=0.6] {$\x$};

\foreach \y in {-1}
\draw ({2pt/(\r)}, \y) -- ({-2pt/(\r)}, \y) node[anchor=east, scale=0.6] {$\y$};

\node[scale=0.8] at ({-0.3/(\r)}, {-0.3/(\r)}) {$O$};

\end{tikzpicture}
\end{document}