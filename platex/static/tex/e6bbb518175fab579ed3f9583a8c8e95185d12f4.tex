\documentclass{article}
\usepackage{tikz, comment}
\usepackage{pifont}
\usepackage{fontspec}
\usetikzlibrary{arrows, decorations.markings, decorations.pathreplacing}
\begin{comment}
:Title: Not defined yet
:Slug: No name yet

Description Here.........
\end{comment}
\begin{document}\centering

\def\rx{1}
\def\ry{1}
\def\a{1}

\begin{tikzpicture}[>=latex, xscale=.5*(\rx), yscale=.5*(\ry)] 

\draw[xstep=1cm,ystep=1cm,color=gray!20] ({-5*(\a)-0.01}, {-3*(\a)}) grid ({5*(\a)}, {3*(\a)});

\draw[->] ({-5*(\a)}, 0) -- ({5*(\a)+0.3/(\rx)},0) node[right] {\small$x$};
\draw[->] (0,{-3*(\a)}) -- (0, {3*(\a)+0.3/(\ry)}) node[above] {\small$y$};

\clip[] ({-5*(\a)}, {-3*(\a)}) rectangle ({5*(\a)}, {3*(\a)});

\foreach \p/\q in {0/{0.5*pi-0.15}, 0/{-0.5*pi+0.15}, {pi}/{0.5*pi+0.15}, {-pi}/{-0.5*pi-0.15}}
\draw[purple, thick, samples=100, smooth, domain=\p:\q, variable=\t] 
	plot ({(\a)/(cos((\t) r))^3}, {(\a)*(tan((\t) r))^3});

\draw ({-\a}, {2pt/(\ry)}) -- ({-\a}, {-2pt/(\ry)}) node[anchor=north, xshift=-5, scale=0.6] {$-a$};
\draw ({\a}, {2pt/(\ry)}) -- ({\a}, {-2pt/(\ry)}) node[anchor=north, scale=0.6] {$a$};

\draw ({2pt/(\rx)}, {\a}) -- ({-2pt/(\rx)}, {\a}) node[anchor=east, scale=0.6] {$a$};
\draw ({2pt/(\rx)}, {-\a}) -- ({-2pt/(\rx)}, {-\a}) node[anchor=east, scale=0.6] {$-a$};

\foreach \p/\q in {{-\a}/0, {\a}/0} 
\draw[purple, fill, xscale=1/\rx, yscale=1/\ry]  ({(\p)*(\rx)}, {(\q)*(\ry)}) circle({0.05});	

\node[scale=0.8] at ({-0.3/(\rx)}, {-0.3/(\ry)}) {$O$};

\end{tikzpicture}
\end{document}