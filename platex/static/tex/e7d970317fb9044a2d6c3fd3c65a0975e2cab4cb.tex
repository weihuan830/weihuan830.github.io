\documentclass{article}
\usepackage{tikz, comment}
\usepackage{pifont}
\usepackage{fontspec}
\usetikzlibrary{arrows, decorations.markings, decorations.pathreplacing}
\begin{comment}
:Title: Not defined yet
:Slug: No name yet

Description Here.........
\end{comment}
\begin{document}\centering

\def\rx{0.85}
\def\ry{0.85}

\begin{tikzpicture}[>=latex, xscale=.5*(\rx), yscale=.5*(\ry)] 

\draw[xstep=1cm,ystep=1cm,color=gray!20] ({-1-0.01}, -8) grid (8, 1);

\draw[->] (-1, 0) -- ({8+0.3/(\rx)},0) node[right] {\small$x$};
\draw[->] (0,-8) -- (0, {1+0.3/(\ry)}) node[above] {\small$y$};

\clip[] (-1, -8) rectangle (8, 1);

\foreach \p/\q in {0.01/0.99, 10/1.01}
\draw[purple, thick, samples=100, smooth, domain=\p:\q, variable=\t] 
	plot ({(\t)*ln(\t)}, {(ln(\t))/(\t)}) -- (0, 0);

\foreach \p/\q in {0/10}
\draw[teal, dashdotted, samples=100, smooth, domain=\p:\q, variable=\x] 
	plot ({\x}, {-\x});

\foreach \x in {1, 5, 6}
\draw (\x, {2pt/(\ry)}) -- (\x, {-2pt/(\ry)}) node[anchor=north, scale=0.6] {$\x$};
\draw ({e}, {2pt/(\ry)}) -- ({e}, {-2pt/(\ry)}) node[anchor=north, scale=0.6] {$e$};

\foreach \y in {-1, -5, -6}
\draw ({-2pt/(\rx)}, \y) -- ({2pt/(\rx)}, \y) node[anchor=west, scale=0.6] {$\y$};
\draw ({-2pt/(\rx)}, {-e}) -- ({2pt/(\rx)}, {-e}) node[anchor=west, scale=0.6] {$-e$};

\foreach \p/\q in {}
\draw[purple, fill=white, xscale=1/\rx, yscale=1/\ry]  ({(\p)*(\rx)}, {(\q)*(\ry)}) circle({0.075});	

\foreach \p/\q in {{e}/{1/e}, {-1/e}/{-e}, {-sqrt(2)*e^(-sqrt(2))}/{-sqrt(2)*e^(sqrt(2))}, {sqrt(2)*e^(sqrt(2))}/{sqrt(2)/e^(sqrt(2))}}
\draw[purple, fill, xscale=1/\rx, yscale=1/\ry]  ({(\p)*(\rx)}, {(\q)*(\ry)}) circle({0.05});	

\node[scale=0.8] at ({-0.3/(\rx)}, {0.3/(\ry)}) {$O$};

\end{tikzpicture}
\end{document}