\documentclass{article}
\usepackage{tikz, comment}
\usepackage{pifont}
\usepackage{fontspec}
\usetikzlibrary{arrows, decorations.markings, decorations.pathreplacing}
\begin{comment}
:Title: Not defined yet
:Slug: No name yet

Description Here.........
\end{comment}
\begin{document}\centering

\def\rx{1.1}
\def\ry{1.1}

\begin{tikzpicture}[>=latex, xscale=.5*(\rx), yscale=.5*(\ry)] 

\draw[xstep=1cm,ystep=1cm,color=gray!20] ({-6-0.01}, -2) grid (6, 5);

\draw[->] (-6, 0) -- ({6+0.3/(\rx)},0) node[right] {\small$x$};
\draw[->] (0,-2) -- (0, {5+0.3/(\ry)}) node[above] {\small$y$};

\clip[] (-6, -2) rectangle (6, 5);

\foreach \p/\q in {0/-6, 0/6}
\draw[purple, thick, samples=100, smooth, domain=\p:\q, variable=\x] 
	plot ({\x}, {rad(acos( (1-(\x)^2)/(1+(\x)^2) )) });

\draw[densely dotted, opacity=0.5] (-6, {pi}) -- (6, {pi});
\node[above, opacity=0.5, scale=0.6] at (-4, {pi}) {$y=\pi$};
	
\foreach \x in {-1, 1}
\draw (\x, {2pt/(\ry)}) -- (\x, {-2pt/(\ry)}) node[anchor=north, scale=0.6] {$\x$};

\foreach \y in {-1, 2, 4}
\draw ({2pt/(\rx)}, \y) -- ({-2pt/(\rx)}, \y) node[anchor=east, scale=0.6] {$\y$};

\foreach \p/\q in {0/0}
\draw[purple, fill, xscale=1/\rx, yscale=1/\ry]  ({(\p)*(\rx)}, {(\q)*(\ry)}) circle({0.05});	

\node[scale=0.8] at ({-0.3/(\rx)}, {-0.3/(\ry)}) {$O$};

\end{tikzpicture}
\end{document}