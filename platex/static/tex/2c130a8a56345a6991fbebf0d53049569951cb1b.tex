\documentclass{article}
\usepackage{tikz, comment}
\usepackage{pifont}
\usepackage{fontspec}
\usetikzlibrary{arrows, decorations.markings, decorations.pathreplacing}
\begin{comment}
:Title: Not defined yet
:Slug: No name yet

Description Here.........
\end{comment}
\begin{document}\centering

\def\r{0.75}

\begin{tikzpicture}[>=latex, xscale=.5*(\r), yscale=.5*(\r)] 

\draw[xstep=1cm,ystep=1cm,color=gray!20] (-3, -3) grid (8, 8);

\draw[->] (-3, 0) -- ({8+0.3/(\r)},0) node[right] {\small$x$};
\draw[->] (0,-3) -- (0, {8+0.3/(\r)}) node[above] {\small$y$};

\clip[] (-3, -3) rectangle (8, 8);

\foreach \p/\q in {8/0.01}
\draw[purple, thick, samples=100, smooth, domain=\p:\q, variable=\x] 
	plot ({\x}, {(\x -3)*sqrt(\x)}) -- (0, 0);
		
\foreach \p/\q in {0/0, 1/-2}
\draw[purple, fill]  ({(\p)}, {(\q))}) circle({0.05/(\r)});	
	
\foreach \x in {1, 3}
\draw (\x, {2pt/(\r)}) -- (\x, {-2pt/(\r)}) node[anchor=north, scale=0.6] {$\x$};

\foreach \y in {-2}
\draw ({2pt/(\r)}, \y) -- ({-2pt/(\r)}, \y) node[anchor=east, scale=0.6] {$\y$};

\node[scale=0.8] at ({-0.3/(\r)}, {-0.3/(\r)}) {$O$};

\end{tikzpicture}
\end{document}