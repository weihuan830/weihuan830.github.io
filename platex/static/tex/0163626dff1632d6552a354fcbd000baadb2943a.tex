\documentclass{article}
\usepackage{tikz, comment}
\usepackage{pifont}
\usepackage{fontspec}
\usetikzlibrary{arrows, decorations.markings, decorations.pathreplacing}
\begin{comment}
:Title: Not defined yet
:Slug: No name yet

Description Here.........
\end{comment}
\begin{document}\centering

\def\rx{1}
\def\ry{1}
\def\a{1}

\begin{tikzpicture}[>=latex, xscale=.5*(\rx), yscale=.5*(\ry)] 

\draw[xstep=1cm,ystep=1cm,color=gray!20] ({-4*(\a)-0.01}, {-1*(\a)}) grid ({4*(\a)}, {5*(\a)});

\draw[->] ({-4*(\a)}, 0) -- ({4*(\a)+0.3/(\rx)},0) node[right] {\small$x$};
\draw[->] (0,{-1*(\a)}) -- (0, {5*(\a)+0.3/(\ry)}) node[above] {\small$y$};

\clip[] ({-4*(\a)}, {-1*(\a)}) rectangle ({4*(\a)}, {5*(\a)});

\foreach \p/\q in {0/5, -5/0}
\draw[purple, thick, samples=100, smooth, domain=\p:\q, variable=\t] 
	plot ({(\a)*(sinh(\t) - \t)}, {(\a)*(cosh(\t) - 1)});

\draw ({-\a}, {2pt/(\ry)}) -- ({-\a}, {-2pt/(\ry)}) node[anchor=north, xshift=-5, scale=0.6] {$-a$};
\draw ({\a}, {2pt/(\ry)}) -- ({\a}, {-2pt/(\ry)}) node[anchor=north, scale=0.6] {$a$};

\draw ({2pt/(\rx)}, {2*\a}) -- ({-2pt/(\rx)}, {2*\a}) node[anchor=east, scale=0.6] {$2a$};

\foreach \p/\q in {0/0} 
\draw[purple, fill, xscale=1/\rx, yscale=1/\ry]  ({(\p)*(\rx)}, {(\q)*(\ry)}) circle({0.05});	

\node[scale=0.8] at ({-0.3/(\rx)}, {-0.3/(\ry)}) {$O$};

\end{tikzpicture}
\end{document}