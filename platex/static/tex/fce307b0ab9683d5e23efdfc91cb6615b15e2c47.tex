\documentclass{article}
\usepackage{tikz, comment}
\usepackage{pifont}
\usepackage{fontspec}
\usetikzlibrary{arrows, decorations.markings, decorations.pathreplacing}
\begin{comment}
:Title: Not defined yet
:Slug: No name yet

Description Here.........
\end{comment}
\begin{document}\centering

\def\rx{1}
\def\ry{1.25}

\begin{tikzpicture}[>=latex, xscale=.5*(\rx), yscale=.5*(\ry)] 

\draw[gray!20, fill=green!5] (0, -3) --++ ({1*pi}, 0) --++ (0, 6) --++ ({-1*pi}, 0) -- cycle;
\draw[gray!20, fill=purple!5] (0, -3) --++ ({-1*pi}, 0) --++ (0, 6) --++ ({1*pi}, 0) -- cycle;

\draw[xstep=0.785398cm,ystep=0.5cm,color=gray!20] ({-3*pi-0.01}, -3) grid ({3*pi}, 3);

\draw[->] ({-3*pi}, 0) -- ({3*pi+0.3/(\rx)},0) node[right] {\small$x$};
\draw[->] (0, -3) -- (0, {3+0.3/(\ry)}) node[above] {\small$y$};

\clip[] ({-3*pi}, -3) rectangle ({3*pi}, 3);

\foreach \k in {-2, 0, 2}
{
\foreach \p/\q in {{(\k)*pi-pi}/{(\k)*pi-3/4*pi-0.1}, {(\k)*pi-3/4*pi+0.1}/{(\k)*pi-1/4*pi-0.1}, {(\k)*pi-1/4*pi+0.1}/{(\k)*pi+1/4*pi-0.1}, {(\k)*pi+1/4*pi+0.1}/{(\k)*pi+3/4*pi-0.1}, {(\k)*pi+3/4*pi+0.1}/{(\k)*pi+pi}}
\draw[purple, thick, samples=100, smooth, domain=\p:\q, variable=\x] 
	plot ({\x}, {cos(\x r)/cos((2*(\x)) r)});
}

\foreach \k in {-2, 0, 2}
{
\draw[densely dotted] ({(\k)*pi-3/4*pi}, -3) -- ({(\k)*pi-3/4*pi}, 3);
\draw[densely dotted] ({(\k)*pi-1/4*pi}, -3) -- ({(\k)*pi-1/4*pi}, 3);
\draw[densely dotted] ({(\k)*pi+1/4*pi}, -3) -- ({(\k)*pi+1/4*pi}, 3);
\draw[densely dotted] ({(\k)*pi+3/4*pi}, -3) -- ({(\k)*pi+3/4*pi}, 3);
}

\draw ({1/2*pi}, {2pt/(\ry)}) -- ({1/2*pi}, {-2pt/(\ry)}) node[anchor=north, scale=0.6] {$\frac{\pi}{2}$};
\draw ({1*pi}, {2pt/(\ry)}) -- ({1*pi}, {-2pt/(\ry)}) node[anchor=north, scale=0.6] {$\pi$};
\draw ({2*pi}, {2pt/(\ry)}) -- ({2*pi}, {-2pt/(\ry)}) node[anchor=north, scale=0.6] {$2\pi$};
\draw ({-2*pi}, {2pt/(\ry)}) -- ({-2*pi}, {-2pt/(\ry)}) node[anchor=north, scale=0.6] {$-2\pi$};

\foreach \y in {-1, 1}
\draw ({2pt/(\rx)}, \y) -- ({-2pt/(\rx)}, \y) node[anchor=east, scale=0.6] {$\y$};

\foreach \p/\q in {0/1, {1/2*pi}/0, {pi}/-1}
\draw[purple, fill, xscale=1/\rx, yscale=1/\ry]  ({(\p)*(\rx)}, {(\q))*(\ry)}) circle({0.05});	

\node[scale=0.8] at ({-0.3/(\rx)}, {-0.3/(\ry)}) {$O$};

\end{tikzpicture}
\end{document}