\documentclass{article}
\usepackage{tikz, comment}
\usepackage{pifont}
\usepackage{fontspec}
\usetikzlibrary{arrows, decorations.markings, decorations.pathreplacing}
\begin{comment}
:Title: Not defined yet
:Slug: No name yet

Description Here.........
\end{comment}
\begin{document}\centering

\def\rx{2.5}
\def\ry{2.5}

\begin{tikzpicture}[>=latex, xscale=.5*(\rx), yscale=.5*(\ry)] 

\draw[gray!20, fill=green!5] (0, 0) -- (1.5, 1.5) --++ (-1.5, 0) -- cycle;

\draw[xstep=0.5cm,ystep=0.5cm,color=gray!20] ({-1.5-0.01}, -1.5) grid (1.5, 1.5);

\draw[->] (-1.5, 0) -- ({1.5+0.3/(\rx)},0) node[right] {\small$x$};
\draw[->] (0,-1.5) -- (0, {1.5+0.3/(\ry)}) node[above] {\small$y$};

\clip[] (-1.5, -1.5) rectangle (1.5, 1.5);

\foreach \p/\q in {-1/1}
{
\draw[purple, thick, samples=100, smooth, domain=\p:\q, variable=\t] 
	plot ({(\t)*(((\t)^2+1)/((\t)^4+1))^(1/2)}, {(1)*(((\t)^2+1)/((\t)^4+1))^(1/2)}); 
\draw[purple, thick, samples=100, smooth, domain=\p:\q, variable=\t] 
	plot ({(1)*(((\t)^2+1)/((\t)^4+1))^(1/2)}, {(\t)*(((\t)^2+1)/((\t)^4+1))^(1/2)}); 
}

\begin{scope}[yscale=-1,xscale=1]
\foreach \p/\q in {-1/1}
\draw[purple, thick, samples=100, smooth, domain=\p:\q, variable=\t] 
	plot ({(\t)*(((\t)^2+1)/((\t)^4+1))^(1/2)}, {(1)*(((\t)^2+1)/((\t)^4+1))^(1/2)}); 
\end{scope}

\begin{scope}[yscale=1,xscale=-1]
\foreach \p/\q in {-1/1}
\draw[purple, thick, samples=100, smooth, domain=\p:\q, variable=\t] 
	plot ({(1)*(((\t)^2+1)/((\t)^4+1))^(1/2)}, {(\t)*(((\t)^2+1)/((\t)^4+1))^(1/2)}); 
\end{scope}

\foreach \p/\q in {-2/2}
{
\draw[teal, dashdotted, samples=100, smooth, domain=\p:\q, variable=\x] 
	plot ({\x}, {\x});
\draw[teal, dashdotted, samples=100, smooth, domain=\p:\q, variable=\x] 
	plot ({\x}, {-\x});
}	

\foreach \x in {1}
\draw (\x, {2pt/(\ry)}) -- (\x, {-2pt/(\ry)}) node[anchor=north, xshift=-3, scale=0.6] {$\x$};
\draw (0.5, {2pt/(\ry)}) -- (0.5, {-2pt/(\ry)}) node[anchor=north, scale=0.6] {$\frac{1}{2}$};

\foreach \y in {1}
\draw ({2pt/(\rx)}, \y) -- ({-2pt/(\rx)}, \y) node[anchor=east, yshift=3, scale=0.6] {$\y$};

\foreach \p/\q in {}
\draw[purple, fill=white, xscale=1/\rx, yscale=1/\ry]  ({(\p)*(\rx)}, {(\q)*(\ry)}) circle({0.075});	

\foreach \p/\q in {0/1, {1/sqrt(2)}/{1/2^(1/4)/sqrt(2-sqrt(2))}, 0.353785/1.04861}
\draw[purple, fill, xscale=1/\rx, yscale=1/\ry]  ({(\p)*(\rx)}, {(\q)*(\ry)}) circle({0.05});	

\node[scale=0.8] at ({-0.3/(\rx)}, {-0.3/(\ry)}) {$O$};

\end{tikzpicture}
\end{document}