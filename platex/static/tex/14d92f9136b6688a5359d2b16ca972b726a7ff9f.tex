\documentclass{article}
\usepackage{tikz, comment}
\usepackage{pifont}
\usepackage{fontspec}
\usetikzlibrary{arrows, decorations.markings, decorations.pathreplacing}
\begin{comment}
:Title: Not defined yet
:Slug: No name yet

Description Here.........
\end{comment}
\begin{document}\centering

\def\r{0.75}

\begin{tikzpicture}[>=latex, xscale=.5*(\r), yscale=.5*(\r)] 

\draw[xstep=1cm,ystep=1cm,color=gray!20] (-5, -5) grid (5, 5);

\draw[->] (-5, 0) -- ({5+0.3/(\r)},0) node[right] {\small$x$};
\draw[->] (0,-5) -- (0, {5+0.3/(\r)}) node[above] {\small$y$};

\clip[] (-5, -5) rectangle (5, 5);

\foreach \p/\q in {0/{2*sqrt(2)-0.01}}
\draw[purple, thick, samples=100, smooth, domain=\p:\q, variable=\x] 
	plot ({\x}, {sqrt(8*(\x)^2 - (\x)^4)}) -- ({2*sqrt(2)}, 0);
		
\foreach \p/\q in {2/4}
\draw[purple, fill]  ({(\p)}, {(\q))}) circle({0.05/(\r)});	
	
\begin{scope}[yscale=1,xscale=-1]

\foreach \p/\q in {0/{2*sqrt(2)-0.01}}
\draw[purple, thick, samples=100, smooth, domain=\p:\q, variable=\x] 
	plot ({\x}, {sqrt(8*(\x)^2 - (\x)^4)}) -- ({2*sqrt(2)}, 0);
		
\foreach \p/\q in {2/4}
\draw[purple, fill]  ({(\p)}, {(\q))}) circle({0.05/(\r)});	

\end{scope}

\begin{scope}[yscale=-1,xscale=1]

\foreach \p/\q in {0/{2*sqrt(2)-0.01}}
\draw[teal, thick, samples=100, smooth, domain=\p:\q, variable=\x] 
	plot ({\x}, {sqrt(8*(\x)^2 - (\x)^4)}) -- ({2*sqrt(2)}, 0);
		
\foreach \p/\q in {2/4}
\draw[teal, fill]  ({(\p)}, {(\q))}) circle({0.05/(\r)});	
	
\begin{scope}[yscale=1,xscale=-1]

\foreach \p/\q in {0/{2*sqrt(2)-0.01}}
\draw[teal, thick, samples=100, smooth, domain=\p:\q, variable=\x] 
	plot ({\x}, {sqrt(8*(\x)^2 - (\x)^4)}) -- ({2*sqrt(2)}, 0);
		
\foreach \p/\q in {2/4}
\draw[teal, fill]  ({(\p)}, {(\q))}) circle({0.05/(\r)});	

\end{scope}

\end{scope}

\foreach \x in {2, 3}
\draw (\x, {2pt/(\r)}) -- (\x, {-2pt/(\r)}) node[anchor=north, scale=0.6] {$\x$};

\foreach \y in {-4, 4}
\draw ({2pt/(\r)}, \y) -- ({-2pt/(\r)}, \y) node[anchor=east, scale=0.6] {$\y$};

\node[scale=0.8] at ({-0.3/(\r)}, {-0.3/(\r)}) {$O$};

\end{tikzpicture}
\end{document}