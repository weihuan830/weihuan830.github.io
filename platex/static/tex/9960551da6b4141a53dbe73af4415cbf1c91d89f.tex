\documentclass{article}
\usepackage{tikz, comment}
\usepackage{pifont}
\usepackage{fontspec}
\usetikzlibrary{arrows, decorations.markings, decorations.pathreplacing}
\begin{comment}
:Title: Not defined yet
:Slug: No name yet

Description Here.........
\end{comment}
\begin{document}\centering

\def\r{1.75}

\begin{tikzpicture}[>=latex, xscale=.5*(\r), yscale=.5*(\r)] 

\draw[xstep=1cm,ystep=1cm,color=gray!20] (-3, -1) grid (3, 3);

\draw[->] (-3, 0) -- ({3+0.3/(\r)},0) node[right] {\small$x$};
\draw[->] (0,-1) -- (0, {3+0.3/(\r)}) node[above] {\small$y$};

\clip[] (-3, -1) rectangle (3, 3);

\foreach \p/\q in {-3/3}
\draw[purple, thick, samples=100, smooth, domain=\p:\q, variable=\x] 
	plot ({\x}, {(\x) + e^(-\x)});

\foreach \p/\q in {0/3}
\draw[densely dotted, samples=100, smooth, domain=\p:\q, variable=\x] 
	plot ({\x}, {\x+0});
\node[below, opacity=0.5, scale=0.6, rotate=45] at (1.5, 1.5) {$y=x$};
	
\foreach \x in {1}
\draw (\x, {2pt/(\r)}) -- (\x, {-2pt/(\r)}) node[anchor=north, scale=0.6] {$\x$};

\foreach \y in {1}
\draw ({2pt/(\r)}, \y) -- ({-2pt/(\r)}, \y) node[anchor=east, yshift=3, scale=0.6] {$\y$};

\foreach \p/\q in {0/1}
\draw[purple, fill]  ({(\p)}, {(\q))}) circle({0.05/(\r)});	

\node[scale=0.8] at ({-0.3/(\r)}, {-0.3/(\r)}) {$O$};

\end{tikzpicture}
\end{document}